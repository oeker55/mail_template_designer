# Netlify build configuration
[build]
  command = "npm run build"
  publish = "dist"

# SPA routing - tüm route'ları index.html'e yönlendir
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
